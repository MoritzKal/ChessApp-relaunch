{
  "title": "Dataset Quality",
  "uid": "dataset-quality",
  "timezone": "browser",
  "schemaVersion": 38, "version": 1,
  "panels": [
    {"type":"table","title":"Rows per Dataset","targets":[{"expr":"sum by (dataset_id) (chs_dataset_rows)"}]},
    {"type":"stat","title":"Invalid Rows (Δ15m)","targets":[{"expr":"sum by (dataset_id) (increase(chs_dataset_invalid_rows_total[15m]))"}]},
    {"type":"stat","title":"Export p95 (s)","targets":[{"expr":"histogram_quantile(0.95, sum by (le) (rate(chs_dataset_export_duration_seconds_bucket[5m])))"}]},
    {"type":"table","title":"Invalid Rows (Top N, 24h)","targets":[{"expr":"topk(10, sum by (dataset_id) (increase(chs_dataset_invalid_rows_total[24h])))"}]}
  ]
}
